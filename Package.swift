// swift-tools-version:5.1

import PackageDescription

let package = Package(
    name: "SwiftUILineChart",
    products: [
        .library(
            name: "SwiftUILineChart",
            targets: ["SwiftUILineChart"]),
    ],
    targets: [
        .target(
            name: "SwiftUILineChart",
            dependencies: []),
    ]
)
